import pandas as pd
import time
import random

def validName(df):
  df = pd.read_csv(df)
  m =0
  for _ in range(len(df)):
    m+=1
    wikipedia.summary(df.ScientificName[m], sentences = 2)
    time.sleep(random.choice(range(1,10))

validName(df ='')
